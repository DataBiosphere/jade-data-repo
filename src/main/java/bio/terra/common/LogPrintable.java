package bio.terra.common;

/**
 * Interface created to have a consistent name for a method to print values to logs and error
 * messages
 */
public interface LogPrintable {

  String toLogString();
}
