databaseChangeLog:
  - changeSet:
      id: datasetUniqueAsset
      author: myessail
      changes:
#        - sql:
#            comment: populate the sharedlock column with empty array
#            sql: UPDATE asset_specification SET name = '{}'
        - addUniqueConstraint:
            catalogName: datarepo
            columnNames: dataset_id, name
            constraintName: dataset_unique_asset
            schemaName: public
            tableName: asset_specification
