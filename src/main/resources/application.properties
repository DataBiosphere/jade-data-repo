springfox.documentation.swagger.v2.path=/api-docs
server.contextPath=
server.port=8080
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false
spring.liquibase.enabled=false

# these application properties are filled in from the gradle.properties file properties of the same name
db.datarepo.uri=${dbDatarepoUri}
db.datarepo.username=${dbDatarepoUsername}
db.datarepo.password=${dbDatarepoPassword}
db.datarepo.changesetFile=${dbDatarepoChangesetFile}
db.stairway.uri=${dbStairwayUri}
db.stairway.username=${dbStairwayUsername}
db.stairway.password=${dbStairwayPassword}
db.stairway.changesetFile=${dbStairwayChangesetFile}
