name: Run Gradle on PRs
on: pull_request
jobs:
  gradle:
    strategy:
      matrix:
        os: [ ubuntu-latest ]
    runs-on: ${{ matrix.os }}
    steps:
    - name: 'Checkout code'
      uses: actions/checkout@v2
    - name: 'Install java'
      uses: actions/setup-java@v1
      with:
        java-version: 8
    - name: 'Configure gradlew'
      run: |
        chmod +x gradlew
        ls
    - name: 'Install gradle'
      uses: eskatos/gradle-command-action@v1
    - name: 'Run gradlew'
      run: |
        ./gradlew assemble
