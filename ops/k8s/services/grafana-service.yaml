kind: Service
apiVersion: v1
metadata:
  name: grafana-service
spec:
  ports:
  - protocol: TCP
    name: http
    port: 3000
    targetPort: 3000
  type: NodePort
  selector:
    service: grafana
    project: datarepo
